import java.util.List;

public class Parser {

    Storage storage;
    public Parser(Storage storage) {
        this.storage = storage;
    }

    /*public List<String> processCommand(String input) throws AngelaException {
        List<String> command;
        command = splitInput(input);
        return false;
    }*/
}
